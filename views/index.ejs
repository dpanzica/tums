<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h2> Welcome to Tums2U! </h2>
      
    <p>This website allows people to buy tums online quick and easy.</p>
      
      <p> Date with To String: <%= Date(Date.now()).toString() %> </p>
      
      
<% if (products.length > 0) {%>
      	<% products.forEach((product, index) => { %>
<product>
	<table>
                        <tr>
<td><%= product.productname %></td>
<td><%= product.product_id %></td>
</tr>
                        <tr>
<td><%= product.productimage %></td>
<td><%= product.status %></td>
</tr>
<tr>
<td><%= product.saleprice %></td>
<td><b>Add To Cart</b></td>
                        </tr>
				</table>
            </product>
			
                    <% }); %>
        <% } else { %>
            <p>No Products</p>
        <% } %>

      
  </body>
</html>
